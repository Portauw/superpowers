# Accessibility Checklist

Canonical accessibility checklist will land with the `frontend-ui` skill at `skills/frontend-ui/accessibility-checklist.md`. After the frontend-ui skill is merged into the integration branch, the integration step will replace this file with cross-references to the canonical version (or merge them).
