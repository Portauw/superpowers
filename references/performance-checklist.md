# Performance Checklist

Canonical performance checklist will land with the `performance` skill at `skills/performance/performance-checklist.md`. After the performance skill is merged into the integration branch, the integration step will replace this file with cross-references to the canonical version (or merge them).
